'use strict';

describe('myApp.upload module', function() {

  beforeEach(module('myApp.upload'));

  describe('PhotoUploadController controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var photoUploadController = $controller('PhotoUploadController');
      expect(photoUploadController).toBeDefined();
    }));

  });
});